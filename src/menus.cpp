#include "globals.h"

// This translation unit intentionally left minimal so all map-related
// implementations reside in src/map.cpp. Keeping this file empty (no
// duplicate definitions) avoids multiple-definition linker errors
// if your build system picks up all .cpp files in the src folder.